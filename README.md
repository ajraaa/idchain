# How To Setup

1. install dependencies (npm install)
2. check frontend/src/config -> create crypto.js & pinata.js
3. connect hardhat localhost to metamask
4. start hardhat
5. deploy (npx hardhat run scripts/deploy.js --network localhost)